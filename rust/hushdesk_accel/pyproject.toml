[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "hushdesk-accel"
version = "0.1.0"
description = "Optional Rust accelerators for HushDesk"
requires-python = ">=3.10"
